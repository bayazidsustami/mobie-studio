// LLM Client
// Formats prompts, compresses the XML UI dump, and parses JSON responses.

pub struct LlmClient {
    // TODO: Define API configuration and HTTP client.
}

impl LlmClient {
    pub fn new() -> Self {
        Self {}
    }
}
