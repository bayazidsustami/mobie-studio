// Frontend (GPUI)
// Manages the chat interface, LLM provider settings, device selection, and displays the generated YAML outputs.

pub struct UiState {
    // TODO: Define state elements for the GPUI frontend.
}
