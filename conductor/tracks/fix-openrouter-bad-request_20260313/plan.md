# Implementation Plan: Fix OpenRouter 400 Bad Request

**Track ID:** fix-openrouter-bad-request_20260313
**Spec:** spec.md
**Created:** 2026-03-13
**Status:** [x] Completed

## Overview

We will update the `RigAgent` to include OpenRouter-specific headers (`HTTP-Referer` and `X-Title`) when communicating with the LLM provider. This ensures OpenRouter correctly identifies and routes requests for models like `hunter-alpha`, preventing the "Stealth" provider routing error.

## Phase 1: Research and Infrastructure [checkpoint: 2025954]

Verify how to inject custom headers into the `rig` OpenAI provider and set up verification tests.

### Tasks

- [x] Task 1.1: Investigate `rig`'s `openai::Client` builder to see if it supports custom headers or a custom `reqwest` client. [2025954]
- [x] Task 1.2: Create a reproduction test case in `tests/` that mocks the LLM endpoint and verifies headers are correctly present in the request. [2025954]

### Verification

- [x] Mock test successfully captures and asserts the presence of `HTTP-Referer` and `X-Title` headers.

## Phase 2: Implementation [checkpoint: 2025954]

Modify the agent initialization to include the required metadata.

### Tasks

- [x] Task 2.1: Update `src/agent/rig_agent.rs` to inject the required OpenRouter headers into the `rig` client. [2025954]
- [x] Task 2.2: (Optional) Update `LlmConfig` if we want these headers to be user-configurable, otherwise use app-level defaults. [2025954]

### Verification

- [x] `RigAgent` continues to initialize correctly for both standard OpenAI and OpenRouter configurations.

## Phase 3: Validation [checkpoint: 2025954]

Ensure stability across the agent's interaction loop.

### Tasks

- [x] Task 3.1: Run `cargo test` to ensure no regressions in agent tools or configuration handling. [2025954]
- [x] Task 3.2: Verify the agent's "thinking" loop starts without immediate HTTP errors in a simulated environment. [2025954]

### Final Verification

- [x] All acceptance criteria met
- [x] Tests passing
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
